#include <stdio.h>

void main(){
	char a[20];
	printf("Enter Your name:");
	fgets(a,20,stdin);
	if(a==Kashaf Lhan)
	{
		printf("Allowed in CLass")
	}
    else {
    	printf("yOU aRE NOT ALLOWED")
    }
}

	
